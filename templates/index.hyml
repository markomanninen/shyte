"<!DOCTYPE html>"
(html
    (head
        (title ~title)
        (link :rel "stylesheet" :href ~(url_for "static" :filename "css/hy.css"))
        ~(head "UTF-8"))
    (body
        (div :class "container"
            ~@(include "templates/content.hyml"))))
